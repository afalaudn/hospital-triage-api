# hospital-triage-api/environment.yml

name: triage-api-env

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - pip
  - pip:
    - fastapi
    - uvicorn
    - pydantic
    - python-dotenv
    - langchain
    - langchain-google-genai